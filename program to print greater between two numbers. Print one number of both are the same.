#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b;
    printf("enter a number: ");
    scanf("%d%d",&a,&b);

    {
        if(a>b)
        printf("%dis greater then %d",a,b);
        else
        printf("%d",b);
    }
    return 0;


}
