#include<stdio.h>
int main()
{
int a;
printf("enter a number: ");
scanf("%d",&a);
{
    if(a>0)
      printf("it is a positive number");
    else if(a==0)
      printf("number is zero");
    else
      printf("number is negative");
}
    return 0;
}
